# ── IMAP 接收 ──
imap_server = imap.exmail.qq.com
imap_port   = 993

# ── SMTP 发送 ──
smtp_server = smtp.exmail.qq.com
smtp_port   = 465

# ── 认证 ──
email =
password =

# ── 日报邮件发送目标（为空则发送给自己） ──
send_to =

# ── AI 后端配置 ──
# 支持任意 OpenAI 兼容服务（OpenAI / DeepSeek / Groq / Ollama / 代理网关）
ai_base_url = https://api.deepseek.com
ai_api_key = sk-your-key-here
ai_model = deepseek-v4-flash

# ── AI 生成参数 ──
ai_thinking = false
ai_temperature = 0.3
ai_max_tokens = 8192